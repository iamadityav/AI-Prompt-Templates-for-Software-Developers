🤖 AI Prompt Templates for Software Developers
A comprehensive collection of AI prompts specifically tailored for software development tasks.

This repository provides a curated list of prompt templates to help software developers leverage AI tools like ChatGPT, GitHub Copilot, Claude, Gemini, and others more effectively.

🎯 How to Use
Each prompt is structured with placeholders—marked in brackets like [language], [framework], or [task]—so you can quickly customize it to your specific project needs.

Example:

Original: Generate a [language] function that performs [task], handling edge cases.

Customized: Generate a Python function that performs email validation, handling edge cases.

📚 Table of Contents
💻 Code Generation & Review

🛠️ Development Tools & Frameworks

🐞 Testing & Debugging

📖 Documentation & Learning

📊 Data Analysis & Machine Learning

🔒 Security & Compliance

☁️ Cloud Computing & DevOps

🏗️ Systems & Architecture

🤝 Collaboration & Communication

📅 Project Management & Planning

💻 Code Generation & Review
Generate a [language] function that performs [task], handling edge cases.

Write a [language] class that implements the [design pattern] with [specific feature].

Convert the following pseudocode into [language] code: [pseudocode snippet].

Refactor the following [language] code to improve [performance/readability/maintainability]: [code snippet].

Review this [language] code for potential bugs and suggest fixes: [code snippet].

Explain what this [language] code does and suggest improvements: [code snippet].

Write a [language] script that parses a [file format] file and extracts [information].

Generate [language] code to integrate [API or library] to accomplish [task].

Create test cases for the following [language] function: [function signature or code].

Optimize the following [language] function for better performance: [code snippet].

Rewrite the following [language] code using best practices: [code snippet].

Translate the following code from [language1] to [language2]: [code snippet].

Write a [language] program that solves [specific algorithmic problem] and explain its approach.

Design a [language] function to [task], and include error handling and documentation.

Generate code for [data structure] operations in [language] (e.g., implementing a queue or stack).

Debug the following [language] code that produces [error message]: [code snippet].

Write a [language] method that implements [feature] given [requirements or inputs].

Convert the following SQL query to [language] code using an appropriate database library: [SQL query].

Create a [language] function to send HTTP requests to [service] with proper authentication.

Extract and summarize the functionality of the following code snippet in [language]: [code].

Write a program in [language] to generate the Fibonacci sequence up to [number].

Identify and correct any syntax errors in the following [language] code: [code snippet].

Implement a [algorithm name] algorithm in [language] with explanatory comments.

Write code to handle exceptions for [scenario] in [language], ensuring robust error handling.

Generate [language] code to connect to [database] and perform CRUD operations securely.

Create a [language] utility script to automate [task, e.g., file backup or data sync].

Refactor the following code to follow [specific coding standard or design principle]: [code snippet].

Write a [language] function that validates [input conditions] (e.g., email format) and returns an error or success.

Implement the [design pattern] pattern in [language] with an example demonstrating its use.

Write [language] code to merge two sorted lists or arrays efficiently.

Generate a [language] regular expression that matches [specific text pattern or format].

Create an abstract class or interface in [language] for [domain concept] with method signatures.

Implement a REST API endpoint in [language]/[framework] that performs [action] on [data].

Generate code to handle file I/O operations for [file format, e.g., CSV, JSON] in [language].

Review the security of this [language] code and suggest improvements: [code snippet].

Write a [language] function to encrypt and decrypt data using [encryption algorithm].

Translate the following function from an imperative style to a functional style in [language]: [code snippet].

Generate code to perform [mathematical operation, e.g., matrix multiplication] in [language].

Implement data validation in [language] for a form that includes [fields] and return structured errors.

Write a [language] command-line tool that [does something specific, e.g., generates a report].

Refactor the following code to reduce its time complexity: [code snippet, e.g., O(n^2) to O(n log n)].

Explain the time complexity of this algorithm and suggest an improvement: [algorithm pseudocode].

Write a program to parse [configuration file format] and output its content in [language] objects.

Generate [language] code to call a REST API and parse the JSON response into objects.

Rewrite the following loop in [language] using a list comprehension or functional programming style: [code snippet].

Create [language] code to generate a [PDF or CSV] report from [data].

Write a [language] function that calculates the [statistical measure, e.g., mean/median] from a list of numbers.

Implement a common sorting algorithm (merge sort or quick sort) in [language] with comments.

Review and improve error messages in the following code for clarity and detail: [code snippet].

Write a [language] script to clean and preprocess data from a [file format].

🛠️ Development Tools & Frameworks
Generate a [framework] project template with [features, e.g., authentication setup].

Explain the lifecycle of a [framework] application and provide an example component or controller.

Write a [language] script using [tool] (e.g., a CLI) to automate [development task].

Create a [tool, e.g., Webpack, Rollup] configuration file for bundling a [framework] application.

Compare [framework1] and [framework2] for building a [application type] application, discussing pros and cons.

Explain how to integrate [service or library] into a [framework] application with code examples.

Generate code for setting up a [CI/CD tool, e.g., GitHub Actions] workflow to build and test a [project].

Write a [framework] component (e.g., a React or Angular component) that implements [specific feature].

Document the steps to create and publish an npm package for a [type of library or utility].

Explain how [package manager] works and write the commands to add dependencies for a [project].

Write a Dockerfile to containerize a [framework or tool] application for deployment.

Generate a [framework] plugin or extension example that adds [specific functionality].

Explain how to use [debugging tool] (e.g., Chrome DevTools) with [IDE] to debug a [language] application.

Write a script to automate code formatting with [formatter tool, e.g., Prettier or Black] on commit.

Generate a [language] script to deploy an application to [cloud or hosting service] using [deployment tool].

Compare [version control systems] (like Git vs SVN) or branching strategies (GitFlow vs trunk) for a team project.

Explain dependency management in [ecosystem, e.g., Python or Node.js] and list common commands or files.

Create a [framework] template for a [type of app, e.g., blog, e-commerce store, portfolio].

Explain how to migrate a project from [framework] version 1 to version 2, outlining key changes.

Write a [language] script using [tool, e.g., CLI] to reset a local database to a known state.

Generate a configuration file (e.g., .gitignore, .dockerignore) customized for a [language] project.

Describe the main features of [CI/CD tool] (e.g., Jenkins, GitLab CI) and show how to configure a basic pipeline.

Write instructions for setting up a [framework] development environment on [operating system].

Explain how to use [linter or testing tool] (e.g., ESLint, Jest) in a [language] project, including a sample config.

Generate code to configure an ORM or database toolkit (like SQLAlchemy, Hibernate) in a [framework] app.

Write a shell command to list and remove [old branches, Docker containers, or images] safely.

Create a script to backup a [framework] project’s database and verify the backup integrity.

Explain how to initialize a new Git repository and commit initial code for [type of project], including .gitignore.

Write a [language] script using [build tool, e.g., Gulp, Grunt, Maven, Gradle] to build and package a project.

Generate instructions to configure [CI/CD platform] to run tests on every pull request push.

Explain the purpose of an artifact repository (like Artifactory or Nexus) and how to publish to it.

Write [language] code to integrate [development tool, e.g., Sentry or Datadog] for error tracking and logging.

Generate a [framework] middleware or plugin that logs [type of events] (e.g., requests, errors).

Explain how to use [tool] to containerize and orchestrate multiple microservices (mention Docker Compose or Kubernetes).

Write a [language] script to automate deployment to [Platform, e.g., Heroku or AWS Lambda] with appropriate CLI commands.

Generate [framework] code for user authentication using [auth library or OAuth].

Explain how to use [Debugger] or [IDE] to step through asynchronous code (e.g., Node.js async/await or Promises).

Write a [configuration] file (like .editorconfig or .prettierrc) for consistent coding style in [project].

Describe the differences between monorepo and polyrepo and outline how to manage each with tools (like Lerna).

Create a [framework] application example that uses [external API or SDK] to fetch and display data.

Explain how to use [package manager, e.g. Yarn or pnpm] with [monorepo tool] (like Lerna) in a large project.

Write a [script or code] to perform linting or static analysis (e.g., pylint, flake8) across the repository.

Generate a [framework] REST API endpoint code to perform [task] with database connectivity example.

Explain how hot-reloading or live-reloading works in [framework] and provide a sample configuration.

Write code to set up CI to run security scans (e.g., using [tool like Snyk or OWASP ZAP]) on the codebase.

Compare build tools (Maven vs Gradle) for a [language] project and show a sample build file for each.

Write a [configuration file] to setup environment variables (e.g., using dotenv or ConfigMap) for a [framework] app.

Explain how to integrate [logging tool, e.g., Log4j, Winston] in a [language]/[framework] project with a config example.

Generate a [framework] front-end component to fetch and display data from a [REST or GraphQL API].

Write a [language] script to automate version bumping and release notes generation (e.g., using semantic release).

Explain how to use [containerization tool] (Docker) to build multi-stage images for [application].

🐞 Testing & Debugging
Write unit tests in [language] using [testing framework] for the following function: [function signature or code].

Generate integration tests for a [API endpoint or service] in [language] using [test tool or framework].

Explain how to use [test framework] to test asynchronous code (e.g., Promises in JavaScript or async in Python).

Find the bug in this [language] code and suggest a fix: [code snippet with a logic error].

Debug the error “[error message]” in [language] and explain possible causes and solutions.

Write a [language] function that validates input and include tests for edge cases (e.g., null or invalid inputs).

Generate a [language] script using [tool, e.g., JMeter or Locust] to perform load testing on [service].

Explain the difference between unit testing and integration testing with examples in [language].

Write code to log function calls and detect where an exception is thrown in [language].

Create a test suite for [module or class] covering at least 80% of the code, and include test names.

Generate a mock [database or service] in [language] to use in testing a [component or feature].

Explain how to use a [debugging tool or IDE] to step through code and find errors in a [language] application.

Write a [language] script to verify that a web page loads successfully and meets performance metrics.

Suggest strategies to handle flaky tests in CI/CD pipelines for [project or environment].

Create a test plan for [application], outlining test cases for each major feature and expected outcomes.

Describe how to reproduce a [browser or server error] and write log statements in [language] to assist debugging.

Explain test-driven development (TDD) and demonstrate with an example in [language], writing the test first.

Write unit tests to cover exceptions and error handling in the following code snippet: [code].

Generate a [language] script to check code formatting standards and fail tests if not formatted (e.g., using Prettier or Black).

Use [mocking library] to simulate [API or service] responses when testing a [component or function].

Explain how to analyze a core dump or crash log in [language] to find where the program crashed.

Write a [tool or script] to benchmark the performance of [function or process] and report time metrics.

Generate a data set with [specific characteristic, e.g., imbalanced classes] for testing a machine learning model.

Describe how to automate end-to-end tests using [tool/framework, e.g., Selenium, Cypress] for [web or mobile] app.

Debug a failing test by interpreting its stack trace and identifying the root cause in [language].

Write a [language] function to sanitize test data and include unit tests to verify it.

Explain how to use continuous testing in [CI tool, e.g., Jenkins] and demonstrate with a simple pipeline configuration.

Create a checklist for manual QA testing of a [web or mobile] application before release.

Generate code to catch and log exceptions with stack traces in [language], including contextual information.

Write tests to ensure thread safety for a [concurrent or parallel operation] in [language], using appropriate test frameworks.

Explain how to mock environment variables or configuration settings in unit tests for [language].

Identify the cause of a memory leak in this [language] code, and suggest how to fix it: [code snippet].

Generate a [language] script to simulate user interactions (clicks, form entries) for testing a [UI component or web page].

Explain the use of assertions in tests and provide examples of assertion statements for [language].

Write code to measure and report code coverage of tests, and suggest ways to improve it in [language].

Debug a [timeout or latency] issue in [service] by adding timing logs in [language] and analyzing the output.

Generate a [language] example of a flaky test and then modify it to make it deterministic.

Explain BDD (Behavior-Driven Development) and create a [scenario or test case] example for [feature].

Write a [language] script to clean up test data in a database after tests have been run.

Use [profiling tool] to profile the performance of [function or process] in [language] and interpret the results.

Write unit tests to mock [external API or service] for a function that sends messages or emails.

Describe how to use a linter as part of the testing process to catch errors before runtime in [language].

Explain how to handle uncaught exceptions in [language] to prevent application crashes, providing code examples.

Write a [language] function to validate JSON schema against an object and write tests for it.

Generate a [language] script to test security aspects (SQL injection, XSS) of a simple API endpoint.

Explain how to configure and use [code coverage tool] (e.g., NYC, Coverage.py) in [language].

Debug a [NullReferenceException/TypeError/IndexError] by analyzing the variable states and call stack in [language].

Write tests for a responsive web component to ensure it renders correctly on different screen sizes and browsers.

Explain how to log and diagnose memory usage issues in a [language] service, including tools or profilers to use.

Create a [language] script to simulate thousands of requests to [service] and record performance metrics.

Explain how to perform A/B testing on a software feature and analyze which variant performs better statistically.

📖 Documentation & Learning
Write a clear README introduction for a [type of project or library], including usage examples.

Generate API documentation for the following endpoints: [endpoint: HTTP method, URL, description].

Explain the [concept, e.g., multithreading or recursion] in simple terms and provide an example in [language].

Create an outline for a tutorial on [topic, e.g., setting up continuous integration] for beginners.

Summarize the key differences between [concept1] and [concept2] for [specific audience, e.g., new developers].

Write doc comments for the following [language] function or class: [function signature or code].

Generate a cheat sheet for [language or framework] covering common commands and syntax.

Create a glossary of terms related to [domain, e.g., web development, AI, databases].

Explain how [algorithm or data structure] works with a step-by-step example and illustrate with data.

Write a quick start guide for deploying a [framework or tool] application on [platform].

Generate quiz questions and answers for [topic, e.g., HTTP, OOP, SQL] to test understanding.

Create a slide deck outline for presenting [technical topic or project overview] to stakeholders.

Summarize the main points of [technical article or concept] in bullet points.

Translate the following technical description into plain English for a non-technical audience: [description].

Write code comments explaining the purpose of the following code snippet: [code].

Generate a list of learning resources (books, websites, courses) for learning [language or technology].

Explain the usage of [technology or tool] in the form of an FAQ (frequently asked questions).

Write a tutorial on how to set up a [development environment or toolchain] for [language or project].

Summarize the process of building and deploying a [application, e.g., web app or service].

Create a list of 10 tips for beginners learning [technology or programming].

Write a script to extract comments from code files and organize them into documentation sections.

Generate example inputs and outputs for the following function signature: [function signature].

Explain a common interview question [question topic] and provide a detailed answer for [language or scenario].

Create a quick reference card for [language syntax or framework commands] for easy lookup.

Write a step-by-step walkthrough of troubleshooting [common issue, e.g., Docker build failure].

Summarize the best practices for [topic, e.g., writing clean code or using REST APIs].

Generate an infographic outline for [technical process, e.g., how a CPU works] (describe the sections).

Write a blog post introduction about [topic, e.g., microservices] and include a simple code example.

Create a metaphor to explain [complex concept, e.g., caching or encryption] to someone new to tech.

Explain how [tool or algorithm] processes data with a simple analogy.

Generate detailed steps for performing [task, e.g., setting up continuous integration] with screenshots described.

Write interview-style flashcards for [topic] to aid memorization of key facts.

Summarize the features of [library or framework] in bullet points, highlighting its strengths.

Create a mind map (in text form) showing relationships between [concepts, e.g., network protocols].

Explain the pros and cons of using [technology or approach] with examples.

Generate an [example API call] usage example in [language] demonstrating a typical workflow.

Write a list of FAQs about [topic] and their answers for a documentation site.

Create an annotated diagram (in text) showing the flow of [process, e.g., user login] step by step.

Explain how [functionality, e.g., caching or threads] is achieved step-by-step in a [project or system].

Write a training checklist for new developers in the team focusing on [process or tech stack].

Summarize a long technical log or error message to identify the core issue and solution.

List and describe 5 common pitfalls when working with [technology or language].

Generate code examples of beginner, intermediate, and advanced usage of [feature, e.g., list comprehension].

Explain [acronym] (e.g., REST, CRUD, ACID) and how it relates to software development.

Write a tutorial on using [tool or library] to accomplish [specific task, e.g., image processing].

Generate a glossary of 20 terms for [specific field, e.g., cybersecurity].

Provide a cheat sheet for Git commands for everyday use.

Explain the steps of [algorithm or process, e.g., quicksort or OAuth handshake] as if teaching a junior developer.

Create a comparative summary table of [two technologies] highlighting their differences.

Write an introductory paragraph for a documentation section on [topic].

Generate example API calls with curl for testing [service or endpoint].

📊 Data Analysis & Machine Learning
Write Python code using [pandas] to load and clean a dataset [dataset description].

Explain the steps to train a [ML algorithm, e.g., decision tree] model on [dataset].

Generate code to visualize [data feature] using [Matplotlib or Seaborn] in [language].

Create a Jupyter notebook outline for analyzing [dataset] with sections for data cleaning, modeling, and evaluation.

Explain the difference between supervised and unsupervised learning with examples.

Write code to split [dataset] into training and testing sets and explain why this is important.

Implement a [model type, e.g., linear regression] in [framework, e.g., scikit-learn] with an explanation of each step.

Describe how to perform cross-validation in [language/framework] with code examples.

Generate an SQL query to aggregate [data column] for summary statistics.

Explain how to interpret the coefficients of a [model type, e.g., linear regression] in [language].

Write code to evaluate [model] accuracy using [metrics, e.g., accuracy, precision, recall] on [dataset].

Create a pipeline to preprocess [text or image] data for machine learning training.

Explain how [algorithm, e.g., k-means] clustering works step-by-step with a diagram in mind.

Generate code to perform feature selection or dimensionality reduction on [data, e.g., PCA or feature importance].

Write a [language] function to calculate precision and recall from prediction outputs.

Explain what regularization is and show how to apply L1 and L2 regularization in [model].

Generate example use cases of [ML technique, e.g., clustering or recommendation] in industry.

Describe how to handle missing data when training a [model], including imputation strategies.

Write code to fine-tune hyperparameters of [model] using [tool, e.g., GridSearchCV or RandomizedSearchCV].

Explain the bias-variance tradeoff and illustrate with an example on [dataset].

Generate code to deploy a trained [model] as a REST API endpoint in [language/framework].

Describe how to use [cloud ML service] (e.g., AWS SageMaker, Google AI Platform) for training models.

Write code to plot a confusion matrix for classification results and interpret it.

Explain how to avoid overfitting when training a neural network, including techniques like dropout.

Generate synthetic data for [specific scenario, e.g., anomaly detection] and explain its characteristics.

Write a [language] script to scrape data from [website or API] for analysis.

Explain how PCA (Principal Component Analysis) reduces dimensionality with an example.

Generate an example of [data structure, e.g., time series] suitable for training a [model type, e.g., RNN].

Describe how to interpret ROC curves and AUC for a binary classifier.

Write code to evaluate clustering quality (e.g., silhouette score) for a [clustering algorithm].

Explain the difference between batch learning and online learning in machine learning.

Generate an outline for a machine learning project workflow, from data collection to deployment.

Create a sample dataset with [characteristic, e.g., skewed distribution] and discuss how it affects modeling.

Describe how to apply transfer learning using a pre-trained [model, e.g., BERT or ResNet].

Write code to serialize and save a trained [model] to disk (e.g., using pickle or joblib).

Explain how to use [tool, e.g., TensorBoard or MLflow] to visualize training progress and metrics.

Generate tips for optimizing [framework or GPU usage] to speed up model training.

Write code to use cross-validation to evaluate a regression model with metrics like RMSE.

Explain the role of an activation function in a neural network and provide examples (ReLU, sigmoid).

Generate a [SQL or BigQuery] query to extract features from a large dataset for training.

Describe how to handle imbalanced classes in classification tasks (e.g., resampling, weighting).

Write code to ensemble multiple [model types] predictions for better accuracy (e.g., voting classifier).

Explain the concept of gradient descent and how it updates model weights with math and example.

Generate a [language] script to connect to a [database] and retrieve data for analysis.

Describe how to detect and remove outliers from a dataset in [language/tool], mentioning methods (z-score, IQR).

Write a [language] function to implement cross-entropy loss for a binary classifier.

Explain the difference between L1 and L2 regularization and when to use each in model training.

Generate example plots to visualize [category] data distributions (e.g., histograms, box plots).

Write code to perform sentiment analysis on text data using [library, e.g., NLTK or TextBlob].

Explain how to validate a machine learning model on streaming data input or in real-time.

Generate a use case for an unsupervised learning technique (e.g., PCA or clustering) in an industry scenario.

🔒 Security & Compliance
Perform a security audit on the following code snippet and identify vulnerabilities: [code snippet].

Explain how to implement OAuth2 authentication in a [language/framework] application with code examples.

Generate code to encrypt user data at rest and in transit in [language], using appropriate libraries.

Write a [language] function that validates user input to prevent SQL injection attacks.

Summarize the steps to ensure GDPR compliance in a web application processing user data.

Explain the concept of XSS (Cross-Site Scripting) and how to mitigate it in [framework], including example code.

Generate a list of best practices for secure password storage (e.g., hashing and salting).

Write a [shell or code] command to scan code for vulnerabilities using [security tool, e.g., OWASP ZAP].

Explain how to implement role-based access control (RBAC) in [framework] with code snippets.

Create a security checklist for deploying a web server (e.g., HTTPS, firewall, updates).

Generate code to log security events and alerts in [language], including timestamp and context.

Explain how HTTPS/TLS works and why it is important for web application security.

Write a [language] snippet to sanitize and validate file uploads (check file type and size).

Summarize the OWASP Top 10 web application vulnerabilities and how to prevent them.

Generate a compliance report outline for PCI-DSS for a merchant e-commerce application.

Explain how to detect and prevent CSRF (Cross-Site Request Forgery) attacks in [language/framework].

Write code to generate and verify JSON Web Tokens (JWT) for API authentication in [language].

Describe best practices for storing API keys and secrets securely (e.g., environment variables, vaults).

Generate a policy document for secure coding standards in a development team.

Explain how CORS (Cross-Origin Resource Sharing) works and how to configure it securely in [framework].

Create a [language] script to perform a vulnerability scan on a web application using a CLI tool.

Write a [language] function to hash passwords using a secure algorithm (e.g., bcrypt or Argon2).

Explain the difference between encryption and hashing with examples of use cases.

Generate a response plan for a data breach in a [industry] context, including notification steps.

Describe how multi-factor authentication (MFA) enhances security and how to implement it in [system].

Write a [framework] middleware to validate input against known security threats (e.g., XSS protection).

Explain how to secure communication between microservices (mutual TLS, service mesh).

Generate a list of security considerations for using [cloud service or technology].

Summarize the requirements for HIPAA compliance in a healthcare software context.

Write code to implement rate limiting in an API endpoint to prevent abuse (e.g., limit to 100 req/min).

Explain the principle of least privilege and how to apply it to database access roles.

Generate a sample secure configuration (e.g., for Nginx or Apache) with recommended settings.

Write a [language] script to rotate encryption keys periodically on a schedule.

Describe how to handle security patches and updates in a DevOps workflow (automation, monitoring).

Explain how to protect against SQL injection when using [ORM or database library], with code examples.

Generate guidelines for secure third-party library management (e.g., regular audits, pin versions).

Write code to implement data validation using [library] and handle exceptions securely.

Explain the concept of 2FA (two-factor authentication) and implement it in [framework].

Generate a checklist for securing cloud resources in [cloud provider] (e.g., IAM, encryption).

Write a threat model for a [type of application] outlining assets, threats, and mitigations.

Explain how auditing and logging contribute to compliance and provide code examples of logging frameworks.

Generate code to implement end-to-end encryption for messages in [language], using public/private keys.

Explain what a security certificate (TLS/SSL) is and how to generate and install one on a server.

Write [configuration] for setting up a firewall or VPN with example rules.

Summarize how public key infrastructure (PKI) works for secure communications in networks.

Generate a plan for a penetration test on a [web application], including scope and test types.

Explain cross-origin restrictions and how to safely allow cross-site requests with proper headers.

Write a [language] function to sanitize HTML input (e.g., removing <script> tags) before rendering.

Describe how to use a Web Application Firewall (WAF) to protect an application, including examples.

Generate a bullet list of common API security best practices (e.g., authentication, input validation).

Explain how to implement security headers (CSP, HSTS, X-Frame-Options) in a [framework], providing examples.

☁️ Cloud Computing & DevOps
Write an Infrastructure-as-Code (Terraform) script to provision a [cloud resource, e.g., AWS EC2 instance] with [configuration].

Generate a [CI/CD tool, e.g., Jenkins or GitHub Actions] pipeline file for building and deploying a [application] to [environment].

Explain how to containerize a [application] with Docker and write the Dockerfile for it.

Create a Kubernetes deployment and service YAML for [application] and explain each section in comments.

Write a script to automatically scale [service] based on CPU usage using [cloud provider’s autoscaling tools].

Explain the concept of serverless computing and give an example with [e.g., AWS Lambda or Google Cloud Functions] including code.

Generate code to deploy a [app] to [cloud platform, e.g., AWS, Azure, GCP] using their CLI (e.g., aws cli).

Create a [cloud] VPC with public and private subnets and explain the reasoning behind the network layout.

Write a [tool, e.g., GitLab CI, Travis CI] pipeline script to run tests, build Docker images, and push them to a container registry.

Explain how to implement a blue-green deployment strategy in [cloud provider or Kubernetes] for zero-downtime updates.

Generate a [framework] project setup for continuous deployment to [cloud service, e.g., AWS Elastic Beanstalk or Azure App Service].

Write a [language] script to backup and restore a [cloud database, e.g., RDS or Cloud SQL] with one-click.

Explain how to monitor [service] on [cloud, e.g., AWS CloudWatch or Azure Monitor] and set up an alert for a threshold.

Generate a Helm chart for deploying [application] and describe each template file (deployment, service, values).

Write commands to migrate a VM from [cloud provider1] to [cloud provider2] (e.g., export/import).

Explain how to use auto-scaling groups in [cloud] to handle variable load for a web application.

Generate a list of [cloud] services (like S3, RDS, Lambda) to set up a [type of application, e.g., data pipeline].

Create a [language] script to automate server health checks and send email alerts if unhealthy.

Explain the role of API gateways (e.g., AWS API Gateway) in microservices and give an example configuration.

Generate IAM policies for [roles, e.g., read-only, admin] in [cloud provider, e.g., AWS IAM policy JSON].

Write [language] code to deploy a serverless function that processes [event, e.g., file upload] using [cloud SDK].

Explain how Infrastructure as Code helps in disaster recovery with an example scenario (e.g., region failure).

Generate a configuration file for [configuration management tool, e.g., Ansible playbook] to provision and configure servers.

Write code for a [cloud service, e.g., Azure Function or AWS Lambda] to log events to a monitoring service (e.g., CloudWatch).

Explain the concept of containers versus VMs and when to use each in deployment.

Generate a script to set up CI/CD with [tool, e.g., Jenkins or GitLab CI] for deploying a microservice application.

Write an example [cloud] policy (e.g., AWS IAM or GCP IAM) that enforces encryption at rest for all databases.

Explain how to deploy a high-availability database in [cloud provider] (e.g., using read replicas or clustering).

Generate a list of commands to securely copy files between two servers using SSH or scp.

Write [language] code using [cloud SDK, e.g., AWS SDK] to create an instance or a resource.

Explain the function of orchestration tools like Kubernetes in modern deployments and how they differ from Docker Compose.

Generate a template for an AWS CloudFormation stack to deploy a simple web application.

Write a [language] script to monitor logs from [cloud service, e.g., AWS CloudWatch or Azure Log Analytics] and send alerts.

Explain how to use Docker Compose to run a multi-container application locally with web and database.

Generate an ARM or Terraform script to create a [specific Azure resource, e.g., Azure App Service or Blob Storage].

Write code to implement a canary deployment for a service using [tool or framework, e.g., Kubernetes or Istio].

Explain how a service mesh (e.g., Istio or Linkerd) works in a microservices architecture and its benefits.

Generate a checklist for secure configuration of [cloud provider, e.g., AWS account] (e.g., root account usage, MFA, S3 policies).

Write a [language] function that triggers on a cloud event (e.g., file upload to S3) and processes the file.

Explain how version control and IaC together streamline environment management and rollback.

Generate a text-based diagram (like Markdown graph) for a multi-tier cloud architecture including [components].

Write a [language] script to clean up unused resources in [cloud] (e.g., terminate idle instances) periodically.

Explain the benefits and drawbacks of multi-cloud deployments for high availability.

Generate a [CI/CD tool] configuration to run database migrations as part of deployment.

Write [language] code to upload build artifacts to an artifact repository (e.g., Nexus or Artifactory).

Explain how to use [monitoring service] to create dashboards for application metrics (e.g., latency, error rate).

Generate an example of a failover plan using [cloud provider] availability zones or regions.

Write a [script] to rotate secrets (like API keys) automatically on a schedule in [service].

Explain how to implement the circuit breaker pattern in microservices to prevent cascading failures.

Generate a list of steps to diagnose a failed deployment in [CI/CD pipeline, e.g., Jenkins or GitLab CI].

Write code to provision a server with [configuration management tool, e.g., Chef or Puppet] for consistent configuration.

🏗️ Systems & Architecture
Design a scalable architecture for [application description, e.g., an e-commerce platform] and explain each component.

Compare monolithic and microservices architectures for [use case] and outline pros and cons for each.

Write an ER (Entity-Relationship) diagram (in text form) for a [database schema] given the following requirements: [requirements].

Generate a sequence diagram (described in text) for [user interaction process, e.g., user login flow].

Explain the CAP theorem and give an example scenario for each case (CP, CA, AP).

Describe how to partition or shard a database to handle high traffic and illustrate with an example.

Design a high-level system architecture for a [mobile app with backend services] and explain the data flow.

Explain how load balancing works and design a solution for [traffic scenario, e.g., seasonal spikes].

Generate a description of a caching strategy (e.g., using Redis or CDN) for [system] to improve performance.

Write [language] code to implement a Singleton pattern and explain its use case.

Compare SQL and NoSQL databases and suggest which is best for [scenario, e.g., logging vs structured data].

Design a RESTful API for [service, e.g., note-taking app] including main endpoints and sample responses.

Explain the role of message queues (e.g., RabbitMQ, Kafka) in a distributed system and when to use them.

Generate a class diagram (described in text) for [module or component].

Design a fallback mechanism (circuit breaker or retry logic) for when [service] becomes unavailable.

Explain the difference between stateful and stateless services and give an example of each.

Write a [language] snippet demonstrating an Observer pattern or Strategy pattern in use.

Describe how to implement multi-tenancy in a database schema or application architecture.

Compare vertical and horizontal scaling and give examples for scaling a database vs web server.

Generate a system diagram with components: [web servers, application servers, databases, load balancers, etc.].

Explain eventual consistency in distributed systems and when it is acceptable (e.g., Amazon S3).

Design an API gateway layer for microservices and explain its benefits (routing, authentication, rate limiting).

Write a [language] function to implement a thread-safe data structure (e.g., synchronized queue).

Explain how a CDN (Content Delivery Network) works and when to use it in system design.

Generate a plan to migrate a legacy monolithic system to a modern cloud-based microservices architecture.

Describe the concept of service discovery and how it could be implemented (e.g., using Consul or Eureka).

Write pseudocode to implement a retry logic with exponential backoff for a failing external call.

Explain design patterns: Factory, Strategy, Decorator and provide code examples in [language].

Design a backup and recovery process for a [data-critical system] including frequency and testing.

Generate a text-based flowchart for user authentication process in a web application.

Explain concurrency vs parallelism with examples in [language], showing how threads or async tasks are used.

Write a [language] snippet for a Pub/Sub messaging scenario (e.g., publishing messages to a topic).

Describe how to handle transactions across multiple microservices (two-phase commit or sagas).

Design a logging and monitoring architecture for [system, e.g., using ELK stack or Prometheus/Grafana].

Compare REST, GraphQL, and gRPC as API communication methods and when to use each.

Write pseudocode for a leader election algorithm (e.g., Raft or Paxos) in a distributed system.

Explain fault tolerance and how to achieve it in [system design, e.g., redundant servers, data replication].

Generate an outline for an architecture document for [project], including key diagrams and descriptions.

Design a database schema for [example project requirement, e.g., user management] with tables and relationships.

Explain how to minimize latency in a real-time application architecture (edge computing, caching).

Write a [language] snippet demonstrating concurrency primitives (threads, async) for a compute task.

Describe the role of caching layers (Redis, Memcached) in system design and example use cases.

Design a multi-region deployment strategy for [application] to improve availability and performance.

Explain how to use CDN and caching to optimize content delivery for [website].

Generate guidelines for choosing between relational vs NoSQL databases for [data type or access pattern].

Write a [language] snippet demonstrating normalization vs denormalization of data structures.

Design an IoT system architecture for collecting and processing sensor data from devices.

Explain how API versioning works and plan how to handle breaking changes in a public API.

Create a fail-safe design to handle service downtime gracefully (e.g., queued tasks, retries, fallback mode).

Describe how to implement a circuit breaker in code to prevent cascading failures in microservices.

Generate a text-based data flow diagram showing how [data] moves through [system components].

🤝 Collaboration & Communication
Write a polite code review comment suggesting improvement for the following code snippet: [code snippet].

Explain [technical concept, e.g., normalized database] to a non-technical stakeholder using an analogy.

Draft an email to your team explaining a new feature request: [feature description].

Generate a meeting agenda for discussing [project topic or milestone].

Summarize a technical document or code diff into bullet points highlighting the main changes.

Write a Slack message to request help with [problem], including what you’ve tried so far.

Outline best practices for writing clear and meaningful commit messages in a collaborative project.

Create a training guide to onboard new developers to [project or tech stack].

Explain how Git branching works in simple terms to a beginner using a metaphor.

Generate a status report update for [project milestone or sprint] with accomplishments and blockers.

Write documentation for [project meeting notes] summarizing decisions made and action items.

Draft a user story for [feature] from an end-user perspective and acceptance criteria.

Explain why [design decision] was made in a project to non-technical stakeholders with justification.

Generate bullet points summarizing the differences in opinions from a debate on [technical topic].

Write guidelines for using issue trackers (Jira, Trello) in a development team (e.g., statuses, priorities).

Draft a polite email to resolve a conflict over [issue] found during code review.

Generate talking points for a daily stand-up meeting for [team/project].

Write an announcement email about [project release or update] to the company, highlighting key features.

Explain the concept of technical debt to a project manager or product owner using an analogy.

Draft a pull request (PR) description template for [repository] including context, changes, and testing steps.

Write a hand-off document summarizing the state of [project/module] for the person taking over the work.

Explain the impact of adopting [new technology or process] on the team’s workflow and productivity.

Generate a list of discussion topics for a retrospective meeting after [sprint/project].

Draft a polite apology and plan after missing a project deadline, outlining next steps.

Write a brief presentation overview of [project status] for stakeholders focusing on progress.

Explain Scrum ceremonies (daily stand-up, sprint planning, sprint review) to a new team member.

Create tips for effective remote collaboration among developers, including tools and communication norms.

Draft a code ownership and responsibilities document for [project] defining who maintains each part.

Explain the purpose of the [project documentation] repository and how developers should use it.

Generate a conflict resolution email for a disagreement in coding style preferences, with a compromise suggestion.

Write an announcement for adopting a new tool [name] including its benefits and any training needed.

Explain the importance of documentation in software projects to a busy executive or manager.

Draft meeting minutes summarizing key points from a [project or technical] session with action items.

Generate a list of tasks and assign roles for a small project team planning session in bullet form.

Write a thank-you note to a colleague who helped fix a critical bug, emphasizing teamwork.

Explain the differences between waterfall and agile to a non-developer, highlighting team impact.

Create guidelines for effective code review etiquette (what to check for, how to give feedback).

Draft a policy for remote work communication (tools, response times, meeting practices).

Explain continuous integration to a non-technical person using a real-world analogy (e.g., assembly line).

Write a brief FAQ section explaining [complex feature or release] to team members.

Generate an email template for requesting a code review before merging a branch.

Explain the concept of "bus factor" to the team and why it’s important to minimize it.

Draft a plan for a hackathon event in the company, including goals, teams, and timeline.

Write bullet points for a newsletter about what the development team achieved last month.

Create a checklist for conducting a successful code review (what to check for, avoid, focus on).

Draft a document policy for using third-party libraries and licenses in projects.

Explain how to give constructive feedback on teammates’ code with examples of phrasing.

Generate a quick poll for team preferences on [tool or practice, e.g., IDE or branch strategy].

Write an outline for a training session on [technology] for the team, including topics and exercises.

Create a slide summarizing the project’s architecture for a cross-functional meeting.

Explain to a customer in layman’s terms how a recently fixed bug resolves their issue.

📅 Project Management & Planning
Outline a project plan for developing [type of software], including milestones, timeline, and deliverables.

Generate user stories for [feature name] with detailed acceptance criteria and definition of done.

Write a risk assessment with mitigation strategies for [project scenario or feature].

Create a project charter for [project], including objectives, scope, stakeholders, and success metrics.

Explain how to use MoSCoW prioritization to categorize a list of features for [project].

Draft a timeline and Gantt chart description for [project phases] (e.g., requirements, development, testing).

Generate a list of key deliverables and milestones for [project goal or release].

Write a budget breakdown for a software development project, including resources like labor and tools.

Describe the roles and responsibilities of [team members, e.g., developers, QA, PM] in [project].

Explain how to perform stakeholder analysis for [project], identifying interest and influence levels.

Create a communication plan for [project], specifying channels, frequency, and stakeholders (e.g., weekly status emails).

Write an outline for a sprint planning meeting for the upcoming sprint, including tasks and story points.

Generate a list of KPIs (Key Performance Indicators) to track for [project objectives].

Draft a project status report highlighting key accomplishments, risks, and next steps.

Explain how to do a SWOT analysis for [project or product], listing strengths, weaknesses, opportunities, threats.

Create a template for a requirements document for [feature or system], including functional and non-functional requirements.

Write a retrospective meeting summary with lessons learned and action items from [sprint/project].

Generate a product roadmap for [product] over the next year, outlining major features per quarter.

Explain how to estimate the time for [task] using techniques like Planning Poker or t-shirt sizing.

Draft a meeting agenda for a project kickoff meeting covering introductions, objectives, and next steps.

Generate a list of potential project risks and their probability and impact (risk matrix style).

Write user acceptance criteria for [feature], describing scenarios of success and failure.

Describe how to break down a complex task into smaller, manageable subtasks (e.g., for sprint backlog).

Create a checklist for product launch readiness, including final testing and stakeholder approvals.

Explain the steps to conduct a feasibility study for implementing [new feature or technology].

Write a postmortem analysis plan after a project phase failure, outlining how to gather insights.

Generate guidelines for prioritizing bugs and enhancements in the backlog using severity and impact.

Create a timeline for rolling out [new version or feature] in phases (alpha, beta, general availability).

Describe the process for integrating customer feedback into the project backlog or roadmap.

Write a stakeholder report template for executive updates, including metrics and key highlights.

Explain the use of velocity in agile teams and how to calculate and use it for planning.

Generate an example of a Kanban board column setup and tasks for [project].

Outline the steps to migrate a project management tool (e.g., from Trello to Jira) smoothly.

Draft the key sections of a project initiation document (PID), such as purpose, scope, and approach.

Create a budget plan with estimates for development, testing, marketing, and contingency for [project].

Explain how to handle scope creep during a project and methods to control it (change control board, backlog).

Generate a checklist for preparing a client demo of [feature] including environment setup.

Write instructions for enforcing a code freeze period before a release (criteria and process).

Describe how to allocate resources (people, time, tools) for a multi-team project to avoid bottlenecks.

Create risk log entries for common scenarios (e.g., staff turnover, requirements change) with response plans.

Outline steps for quality assurance processes in [project], like code reviews, automated tests, and audits.

Explain how to incorporate feedback from a stakeholder review meeting into the project plan.

Generate a template for weekly sprint retrospective feedback collection (e.g., what went well, to improve).

Write a vision and mission statement for a new software product [description of product].

Create milestones and deliverables for a website redesign project from planning to launch.

Explain how to measure project success and define metrics (ROI, user adoption, bug counts).

Draft a summary of lessons learned from [phase or project] and recommendations for future improvement.

Generate a chart description (bar or line chart) showing project progress data (e.g., velocity by sprint).

Write a draft project proposal summary for [idea], including objectives, scope, and benefits.

Create an onboarding plan for a new project manager joining the team, covering key documents and introductions.

Explain how to handle project dependencies (on other teams or external factors) and track them in planning.

🤝 Contributing
Contributions are welcome! If you have a useful prompt template that isn't listed here, feel free to open a pull request.
